import { PasswordVaidatorDirective } from './password-vaidator.directive';

describe('PasswordVaidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordVaidatorDirective();
    expect(directive).toBeTruthy();
  });
});
